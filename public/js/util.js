"use strict";

function qs(elemento){
	return document.querySelector(elemento);
}
function qsa(query) {
	return document.querySelectorAll(query);
}
function crearEl(etiqueta){
	return document.createElement(etiqueta);
}